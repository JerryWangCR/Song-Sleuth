import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    smallText: {
        fontSize: 24,
        color: '#fff',
        textAlign: 'center',
        marginTop: 10,
    },
})